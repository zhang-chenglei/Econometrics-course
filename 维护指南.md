# GitHub课程网站维护指南

> 本指南是《计量经济学：理论与实践》GitHub课程网站的统一维护入口。无论由教师、Codex还是Claude Code维护，开始操作前都应先阅读本文件。

## 1. 项目定位

- **本地仓库**：`~/Documents/EconKB/30_教学/09_计量教材/GitHub课程网站/`
- **GitHub仓库**：<https://github.com/zhang-chenglei/Econometrics-course>
- **公开网站**：<https://zhang-chenglei.github.io/Econometrics-course/>
- **发布方式**：向 `main` 分支推送后，GitHub Actions自动运行Quarto并发布GitHub Pages。

这个文件夹是一个**独立Git仓库**。虽然它放在EconKB的计量教材目录中，但不并入EconKB主仓库的版本历史。

## 2. 三类内容及其权威来源

网站内容来自三条线，修改前必须先判断属于哪一类。

| 内容 | 权威来源 | 正确修改位置 |
|---|---|---|
| 共享教材正文 | `../拆分章节/`及`../图片/`（在 EconKB 主仓库内） | 先修改EconKB教材源文件，再运行同步脚本 |
| 15讲课程页与专题讲义 | `../../01_本学期计量课/中级计量（研究生）/课程知识库/飞书课程版/` | 先修改飞书课程版源文件，再运行同步脚本 |
| GitHub专用页面、公开课件、代码与数据 | 当前仓库 | 直接在当前仓库维护 |

> ⚠️ **课程页的唯一权威源是「中级计量（研究生）」那一份。** `01_本学期计量课/` 下**有三个**同名目录 `课程知识库/飞书课程版/`（中级计量（研究生）、计量应用（经济与金融）、计量应用（互联网金融））；另两份**不参与本网站的生成**——经济与金融由 `_维护/派生工具/derive_course.py` 从中级计量派生，互联网金融是独立课程。改错一份，网站不会变，而你会以为改了。

最重要的原则是：**不要直接修改自动生成的副本。**

以下文件通常由同步脚本生成，手工修改后会在下次同步时被覆盖：

- `index.qmd`
- 除仓库专用页面外的 `course/*.qmd`
- `textbook/*.qmd`
- `assets/images/`
- `_quarto.yml` 中两个 `AUTO-GENERATED` 标记之间的侧边栏
- `llms.txt`

目前的仓库专用页面是：

- `course/updates.qmd`
- `course/materials.qmd`
- `course/basic_econometrics_slides.qmd`（2025—2026秋季学期计量基础课件）

这些页面应同时登记在 `tools/sync_from_econkb.py` 的登记表中，才能稳定进入侧边栏和AI索引。**登记表有两张，别只找一张**：

| 表 | 作用 | 收哪些页 |
|---|---|---|
| `EXTRA_RESOURCE_PAGES` | 挂在「课程资源」区的仓库专用页 | `updates`、`materials` |
| `EXTRA_TOP_LEVEL_PAGES` | **自带一个顶层侧边栏分区**的页；登记项是 `(section, href, title)` 三元组 | `basic_econometrics_slides`（「60｜历年课程课件」） |

注册错表不会报错，只是页面进不了预期的位置。

## 3. 目录地图

```text
GitHub课程网站/
├── _quarto.yml               网站结构与Quarto配置
├── index.qmd                 自动同步的公开主页
├── course/                   课程讲义与仓库专用页面
├── textbook/                 自动同步的教材网页副本
├── assets/images/            自动同步并压缩后的网页图片
├── materials/                随站发布的教学数据与课件
│   ├── slides/               课堂PPT、PDF及补充课件
│   └── data/                 可公开的教学数据
├── tools/                    维护脚本
│   ├── sync_from_econkb.py     从EconKB同步课程和教材
│   ├── sync_student_repo.py    生成学生版代码仓库
│   └── check_materials_code.py 学生代码自包含检查
├── styles.css                网站视觉样式
├── .github/workflows/        GitHub Pages自动发布流程
├── README.md                 项目概要与常用命令
└── 维护指南.md               当前文件
```

`_site/` 是本地构建产物，可以随时重新生成，不是内容源。

**学生代码不在本仓库内**，在独立仓库 `Econometrics-course-materials`，本地检出位于上一层目录（`30_教学/09_计量教材/Econometrics-course-materials/`），由 `tools/sync_student_repo.py` 生成。

## 4. 每次维护前的固定检查

在当前仓库根目录执行：

```bash
git status --short --branch
git pull --ff-only origin main
```

如果状态中已有未提交修改，先确认这些改动来自谁、是否与当前任务重叠。不要覆盖或清理不属于本次任务的文件。

同时确认远端地址没有变化：

```bash
git remote -v
```

## 5. 更新教材或飞书课程内容

### 5.1 先在权威源修改

- 教材章节在 `EconKB/30_教学/09_计量教材/拆分章节/` 修改。
- 课程页在对应飞书课程版源文件中修改，并按飞书维护流程发布。
- 不要直接修改网站中的 `textbook/*.qmd` 或自动同步的课程页。

### 5.2 检查网站是否存在差异

```bash
python3 tools/sync_from_econkb.py \
  --econkb "$HOME/Documents/EconKB" \
  --check
```

`--check`只检查，不写文件。出现“page(s) out of date”说明网站副本需要同步。

⚠️ **`--check` 只比对页面正文**（`index.qmd`、`course/*.qmd`、`textbook/*.qmd`）。它**不检查**：

- `_quarto.yml` 的侧边栏（同步时才重新生成）
- `llms.txt`
- `assets/images/` 里的图片副本

所以 `--check` 报 “All pages up to date” **不等于**网站已与源一致——改完仍要看 `git diff`。（图片缺失会在同步时以 traceback 的形式暴露，不是漂移行。）

### 5.3 正式同步

```bash
python3 tools/sync_from_econkb.py \
  --econkb "$HOME/Documents/EconKB"
```

同步完成后必须查看 `git diff`，重点确认：

- 教材和课程页只出现预期变化；
- 导航层级没有异常；
- GitHub公共站点没有重新出现具体班级的考核和临时通知；
- 仓库专用页面仍然存在；
- `llms.txt` 已包含所有需要Agent读取的页面。

### 5.4 课程页图片路径规则（易错）

课程页引用的图片必须放在**飞书课程版的 `图片/` 目录**下，Markdown 写成：

```markdown
![](图片/<子目录>/文件名.png)
```

例如 `![](图片/lesson02/ch02-fwl-partial-regression.png)`。

**不要使用 `../图片/...` 形式的路径。** 两个工具对图片路径的解析方式**相反**（2026-09-21 实测更正，此前本表写反了）：

| 工具 | 解析方式 | 遇到 `../图片/<子目录>/x.png` |
|---|---|---|
| `tools/sync_from_econkb.py`（网站同步） | 正则 `COURSE_IMAGE_RE` **自带 `(?:\.\./)?` 前缀**，会把 `../` 剥掉再按 `飞书课程版/图片/` 解析 | **能解析，同步成功** |
| `publisher.py`（飞书发布） | `(BASE_DIR / 路径).resolve()`，**不归一化** `../`；且 `课程知识库/图片` 是指向 `09_计量教材/图片/` 的符号链接，那里**没有** `xiaohei/`、`lesson02/`、`monte_carlo/` 这些子目录 | 找不到文件，抛 `FeishuError("图片文件不存在")` |

**所以失败发生在飞书发布阶段，而不是网站同步阶段**——`../图片/...` 的页面能顺利同步上线，直到跑 `publisher.py` 才报错，容易被误判为「网站已经好了就没问题」。**课程页图片一律用 `图片/<子目录>/...`（不带 `../`），两个工具都能解析。**

教材正文中的 `![[文件名]]` 写法只适用于 `textbook/*.qmd`（同步脚本会在 `09_计量教材/图片/` 下按文件名查找），课程页不要使用。

新增图片时，命名沿用所在子目录的既有惯例（如 `lesson02/` 下为 `chNN-主题.png`）。

### 5.5 图片的可访问性 `alt`（别去"补"它）

**全站图片的 `alt` 由同步脚本自动生成，一律不要手工写。**

实测的 Quarto 行为（三种写法对比，2026-09-21）：

| 写法 | 结果 |
|---|---|
| `![](图)` | `<img>` **完全没有 `alt` 属性** |
| `![](图){fig-alt=""}` | `alt=""`（装饰图，读屏会跳过） |
| `![](图){fig-alt="文字"}` | `alt="文字"` |
| `![文字](图)` | 生成**页面上可见的图题**（`<figcaption>`），而 `<img>` **仍然没有 `alt`** |

所以 **`![文字](图)` 是最坏的写法**——它既没补上 alt，又让图题在页面上多显示一遍（首发时就踩过这个坑：26 个教材页出现约 63 个重复图题）。

同步脚本现在**恒输出** `{fig-alt="…"}`：

- **教材侧**：取图片后面那行 `图N-M …` 题注；取不到就输出空值
- **课程侧**：取图片**下一行**非空行里的题注（`图一 …` / `图N-M …`）

装饰图写显式空 `alt=""`，**不要留裸 `![]()`**。改完核验：渲染后的 HTML 里 `<figcaption>` 应为 **0**，`<img>` 缺 `alt` 属性的应为 **0**。

## 6. 上传或更新每讲课件

正式课件统一放在：

```text
materials/slides/
```

补充课件可以再建专题子目录，例如：

```text
materials/slides/basic-econometrics/
```

文件名建议使用稳定的英文和讲次编号，例如：

```text
lecture01-intermediate-econometrics-introduction.pptx
lecture02-regression-review.pdf
```

这样可以避免中文、空格和括号导致链接失效。更新同一讲时优先沿用原文件名，让学生收藏的下载地址保持不变。

上传后的处理步骤：

1. 检查PPTX或PDF能否正常打开、页数是否合理；
2. 将文件复制到固定目录；
3. 在 `course/updates.qmd` 增加或更新下载入口与日期；
4. 补充材料需要独立说明时，新建一个仓库专用 `.qmd` 页面；
5. 新页面若要进入导航和Agent索引，将其加入 `EXTRA_RESOURCE_PAGES`；
6. 完整构建并检查公网链接。

课件文件默认作为下载材料。若希望AI Agent直接理解课件内容，最好同时提供对应的Markdown讲义或Quarto网页版本；只有PPTX或PDF时，不同Agent的读取效果可能不一致。

## 7. 更新代码和数据

**学生用的代码与数据在独立仓库**：<https://github.com/zhang-chenglei/Econometrics-course-materials>。
本仓库的 `materials/` 只保留随站发布的内容：`data/`（教学数据，供 `course/materials.qmd` 直链下载）和 `slides/`（课堂讲义）。**本仓库不再存放学生代码。**

### 学生仓库是怎么来的

它由 `tools/sync_student_repo.py` 生成，**不要手工编辑学生仓库**：

```bash
# 改完 EconKB 源文件后，在本仓库执行
python3 tools/sync_student_repo.py \
  --econkb ~/Documents/EconKB \
  --target ~/Documents/EconKB/30_教学/09_计量教材/Econometrics-course-materials
```

源文件位置：

- 第1—9章：EconKB `30_教学/09_计量教材/code/{python,stata,ai_cards}/`
- 第**12**—15章贯穿案例：EconKB `30_教学/09_计量教材/code/comprehensive_case/`
  （该目录下只有 `第12章`—`第15章` + `共享代码`，**没有第11章**——案例包自述即「第12—15章人工智能试验区综合案例复现包」）
- 数据：本仓库 `materials/data/`

> ⚠️ **第10章与第11章没有随包发布的代码**：`code/python/ch10`—`ch14`、`code/stata/ch10`—`ch14` 目前都是**空目录**，`sync_student_repo.py` 的 `CHAPTERS` 列表也只到 `ch09`。因此 `code/ai_cards/ch10_ai_task_card.md`（该文件**存在**）**不会**被发布到学生包。这是当前的实际状态，不是遗漏的记录——若要发布第10章，需在 `CHAPTERS` 里补 `("ch10", …)` 并把对应脚本补齐。

脚本按教材章节重组、剔除 `legacy/` 旧版文件、把任务卡统一命名为 `AI任务卡.md`、生成 `README.md`，并让脚本与学生源**逐字一致**——两边靠生成关系保持同步，不靠人工纪律。

### 学生版代码必须自包含

学生会把脚本下载到桌面、下载文件夹或U盘再运行，因此：

- 不得读取仓库内部目录（`图片/`、`教材插图` 等）——学生手里没有这些目录；
- 不得依赖脚本所在的目录深度（如 `Path(__file__).parents[3]`）——这类写法不报错，而是在学生解压的位置旁边**凭空新建一个目录**；
- 输出只写在自己脚本同级的 `output/`。

发布前必须确认：

- 没有学生姓名、学号、联系方式等个人信息；
- 没有API密钥、账号密码和本机配置；
- 没有受许可限制、禁止再分发的数据；
- 代码路径不依赖个人电脑的绝对地址，也不依赖仓库内的目录层级；
- 数据和代码至少有一份README说明来源、变量和运行顺序；
- 相关入口已同步更新到 `course/materials.qmd`。

可自动检查：

```bash
python3 tools/check_materials_code.py            # 绝对路径、仓库内部目录 → 报错
python3 tools/check_materials_code.py --strict   # 目录深度依赖也一并报错
python3 tools/sync_student_repo.py --econkb ~/Documents/EconKB --target <学生仓库> --check
```

`check_materials_code.py` 默认扫描同级目录下的 `Econometrics-course-materials`，可用 `--target` 指定其他检出位置。

**三条检查各自的取源与例外**（改这个脚本前必读）：

| 检查 | 级别 | 取源 | 说明 |
|---|---|---|---|
| 写死的本机绝对路径 | error | Python 用 `strip_comment_tokens()`（**只清注释、保留字符串**）；Stata 用 `strip_stata_comments()` | 路径只可能写在字符串里。此前两处共用「连字符串一起清空」的 `strip_comments()`，导致 **Python 侧的这条检查长期空跑**（匹配式里含引号，而引号已被清掉）。2026-09-21 修复 |
| 引用仓库内部目录 | error | `strip_comments()`（清注释与字符串） | 注释里会合法地提到 `图片/教材插图`（记录 `CASE_OUTPUT_DIR` 逃生口），故必须清字符串 |
| 依赖脚本所在目录深度 | warn | 同上 | `Path(__file__).parents[n]` |

⚠️ **`code/comprehensive_case/` 有一条豁免**：该包的 README 明确要求保留仓库目录结构才跑得起来（脚本向上走到仓库根找 `data/semisynthetic/`），所以那里的 `parents[n]` 是**包级契约**，不是疏漏。豁免**只作用于深度这条 warn**，两条 error 级检查对它照旧执行。

因此 `--strict` 的「目录深度依赖也一并报错」这句**对该包不成立**——这是设计，不是 bug。**不要**去"修" `comprehensive_case` 里的 `parents[n]`。

## 8. 新增GitHub专用网页

新页面放在 `course/`，扩展名使用 `.qmd`。如果页面只属于GitHub网站而不属于飞书课程，必须：

1. 在 `tools/sync_from_econkb.py` 的登记表中登记（**两张表，按页面性质选**：一般资源页进 `EXTRA_RESOURCE_PAGES`；需要自带顶层侧边栏分区的进 `EXTRA_TOP_LEVEL_PAGES`，登记项是 `(section, href, title)` 三元组，见 §2）；
2. 需要时在仓库专用的 `course/updates.qmd` 增加入口；若要修改自动生成的 `course/resources.qmd`，必须先修改其飞书课程版源文件再同步；
3. 重新生成 `llms.txt`；
4. 验证同步脚本不会把它视为意外孤立页面。

不要手工修改 `_quarto.yml` 的自动生成区；自动生成区以外的站点标题、顶部导航和样式配置可以按需要修改。

> ⚠️ **改 `_quarto.yml` 的 `format.html.lang` 时必须写 `zh`，不能写 `zh-CN`。**
> Quarto（1.10.18）只带 `_language-zh.yml` 与 `_language-zh-TW.yml` **两个中文包**
> ——**没有 `zh-CN`**（见 `<quarto安装目录>/share/language/`）。写 `zh-CN` 会让语言包
> **静默加载失败**，构建日志出现 `Could not load translations for zh-CN` 与
> `The term Abstract has no translation defined`，并且**小节标题回退成英文**
> （「摘要」变 Abstract）。2026-09-21 修复过一次。
> 该行在 `AUTO-GENERATED` 标记**之外**，属可手改区。改完的验证：`quarto render`
> 的日志里 `grep -ciE "could not load translations|no translation defined"` 应为 **0**。

## 9. 本地构建与检查

正式发布前至少执行：

```bash
quarto render
git diff --check
git status --short
```

验收重点：

- Quarto完整构建成功；
- 新页面存在于 `_site/`；
- 页面中的站内链接指向生成后的 `.html`；
- PDF、PPT、代码和数据被复制到 `_site/materials/`；
- 桌面端和手机端导航没有明显错位；
- 页面没有残留本机绝对路径；
- 下载文件可以从本地构建结果正常读取。

如需边改边看，可运行：

```bash
quarto preview
```

## 10. 提交与发布

确认改动无误后：

```bash
git add <本次修改的文件>
git commit -m "简明说明本次更新"
git push origin main
```

不要用 `git add .` 草率提交不明文件。推送后可查看发布状态：

```bash
gh run list --limit 3
```

只有GitHub Actions显示 `success`，并且公开页面与下载链接返回正常，才算发布完成。

## 11. 出错与回退

- 先用 `git diff` 和 `git log --oneline` 确认问题来自哪次修改。
- 已经推送的错误提交优先使用 `git revert <commit>` 生成可追溯的回退提交。
- 不使用 `git reset --hard`、强制推送或批量删除来处理普通发布错误。
- 如果同步结果异常，先停止提交，检查权威源、`manifest.json` 和同步脚本，不要在大量生成文件上逐个手修。

## 12. Agent接手清单

Codex或Claude Code开始工作时，应按以下顺序：

1. 阅读 `维护指南.md` 和 `README.md`；
2. 查看 `git status`、当前分支、远端与最近提交；
3. 判断任务属于权威源同步还是仓库专用内容；
4. 修改前向用户说明范围、风险和验证方式；
5. 只改与任务相关的文件；
6. 运行同步检查、Quarto构建和链接验证；
7. 用户要求发布时才提交并推送；
8. 发布后核验GitHub Actions与公开网址；
9. 在EconKB当天日志中记录重要结构调整和发布结果。

## 13. 当前关键入口

- 课程主页：<https://zhang-chenglei.github.io/Econometrics-course/>
- 课件与更新：<https://zhang-chenglei.github.io/Econometrics-course/course/updates.html>
- 代码与数据：<https://zhang-chenglei.github.io/Econometrics-course/course/materials.html>
- 2025—2026秋季学期计量基础课件：<https://zhang-chenglei.github.io/Econometrics-course/course/basic_econometrics_slides.html>
- GitHub提交记录：<https://github.com/zhang-chenglei/Econometrics-course/commits/main/>

最后更新：2026-09-21。

> 当前线上版本（截至上述日期）：网站提交 `8bef2d0`、学生包 `33aa488`、CI run `35557732091`（build + deploy 均 success）。**本指南每次修订都要改上面这个日期**——它停在 09-14 的那段时间，正是 09-20/09-21 的三条新规矩（`lang: zh`、图片 `alt`、检查器取源）没能被记录下来的原因。
