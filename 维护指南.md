# GitHub课程网站维护指南

> 本指南是《计量经济学：理论与实践》GitHub课程网站的统一维护入口。无论由教师、Codex还是Claude Code维护，开始操作前都应先阅读本文件。

## 1. 项目定位

- **本地仓库**：`~/Documents/EconKB/30_教学/09_计量教材/GitHub课程网站/`
- **GitHub仓库**：<https://github.com/zhang-chenglei/Econometrics-course>
- **公开网站**：<https://zhang-chenglei.github.io/Econometrics-course/>
- **发布方式**：向 `main` 分支推送后，GitHub Actions自动运行Quarto并发布GitHub Pages。

这个文件夹是一个**独立Git仓库**。虽然它放在EconKB的计量教材目录中，但不并入EconKB主仓库的版本历史。

## 2. 三类内容及其权威来源

网站内容来自三条线，修改前必须先判断属于哪一类。

| 内容 | 权威来源 | 正确修改位置 |
|---|---|---|
| 共享教材正文 | `../拆分章节/`及`../图片/` | 先修改EconKB教材源文件，再运行同步脚本 |
| 15讲课程页与专题讲义 | `../../01_中级计量经济学（应用经济学硕士）/课程知识库/飞书课程版/` | 先修改飞书课程版源文件，再运行同步脚本 |
| GitHub专用页面、公开课件、代码与数据 | 当前仓库 | 直接在当前仓库维护 |

最重要的原则是：**不要直接修改自动生成的副本。**

以下文件通常由同步脚本生成，手工修改后会在下次同步时被覆盖：

- `index.qmd`
- 除仓库专用页面外的 `course/*.qmd`
- `textbook/*.qmd`
- `assets/images/`
- `_quarto.yml` 中两个 `AUTO-GENERATED` 标记之间的侧边栏
- `llms.txt`

目前的仓库专用页面是：

- `course/updates.qmd`
- `course/materials.qmd`
- `course/basic_econometrics_slides.qmd`（2025—2026秋季学期计量基础课件）

这些页面应同时登记在 `tools/sync_from_econkb.py` 的 `EXTRA_RESOURCE_PAGES` 中，才能稳定进入侧边栏和AI索引。

## 3. 目录地图

```text
GitHub课程网站/
├── _quarto.yml               网站结构与Quarto配置
├── index.qmd                 自动同步的公开主页
├── course/                   课程讲义与仓库专用页面
├── textbook/                 自动同步的教材网页副本
├── assets/images/            自动同步并压缩后的网页图片
├── materials/                随站发布的教学数据与课件
│   ├── slides/               课堂PPT、PDF及补充课件
│   └── data/                 可公开的教学数据
├── tools/                    维护脚本
│   ├── sync_from_econkb.py     从EconKB同步课程和教材
│   ├── sync_student_repo.py    生成学生版代码仓库
│   └── check_materials_code.py 学生代码自包含检查
├── styles.css                网站视觉样式
├── .github/workflows/        GitHub Pages自动发布流程
├── README.md                 项目概要与常用命令
└── 维护指南.md               当前文件
```

`_site/` 是本地构建产物，可以随时重新生成，不是内容源。

**学生代码不在本仓库内**，在独立仓库 `Econometrics-course-materials`，本地检出位于上一层目录（`30_教学/09_计量教材/Econometrics-course-materials/`），由 `tools/sync_student_repo.py` 生成。

## 4. 每次维护前的固定检查

在当前仓库根目录执行：

```bash
git status --short --branch
git pull --ff-only origin main
```

如果状态中已有未提交修改，先确认这些改动来自谁、是否与当前任务重叠。不要覆盖或清理不属于本次任务的文件。

同时确认远端地址没有变化：

```bash
git remote -v
```

## 5. 更新教材或飞书课程内容

### 5.1 先在权威源修改

- 教材章节在 `EconKB/30_教学/09_计量教材/拆分章节/` 修改。
- 课程页在对应飞书课程版源文件中修改，并按飞书维护流程发布。
- 不要直接修改网站中的 `textbook/*.qmd` 或自动同步的课程页。

### 5.2 检查网站是否存在差异

```bash
python3 tools/sync_from_econkb.py \
  --econkb "$HOME/Documents/EconKB" \
  --check
```

`--check`只检查，不写文件。出现“page(s) out of date”说明网站副本需要同步。

### 5.3 正式同步

```bash
python3 tools/sync_from_econkb.py \
  --econkb "$HOME/Documents/EconKB"
```

同步完成后必须查看 `git diff`，重点确认：

- 教材和课程页只出现预期变化；
- 导航层级没有异常；
- GitHub公共站点没有重新出现具体班级的考核和临时通知；
- 仓库专用页面仍然存在；
- `llms.txt` 已包含所有需要Agent读取的页面。

### 5.4 课程页图片路径规则（易错）

课程页引用的图片必须放在**飞书课程版的 `图片/` 目录**下，Markdown 写成：

```markdown
![](图片/<子目录>/文件名.png)
```

例如 `![](图片/lesson02/ch02-fwl-partial-regression.png)`。

**不要使用 `../图片/...` 形式的路径。** 两个工具对图片路径的解析方式不同：

| 工具 | 解析方式 | 遇到 `../图片/...` |
|---|---|---|
| `publisher.py`（飞书发布） | `BASE_DIR / 路径`，会跟随符号链接 | 能解析，**发布成功** |
| `tools/sync_from_econkb.py`（网站同步） | `飞书课程版/图片/` + 子路径，不跟随符号链接 | 找不到文件，抛 `FileNotFoundError` |

因此 `../图片/...` 的失败**发生在网站同步阶段，而不是飞书发布阶段**：飞书那边看起来一切正常，直到运行同步脚本才报错，容易被误判为已经处理完毕。**课程页图片一律用 `图片/<子目录>/...`。**

教材正文中的 `![[文件名]]` 写法只适用于 `textbook/*.qmd`（同步脚本会在 `09_计量教材/图片/` 下按文件名查找），课程页不要使用。

新增图片时，命名沿用所在子目录的既有惯例（如 `lesson02/` 下为 `chNN-主题.png`）。

## 6. 上传或更新每讲课件

正式课件统一放在：

```text
materials/slides/
```

补充课件可以再建专题子目录，例如：

```text
materials/slides/basic-econometrics/
```

文件名建议使用稳定的英文和讲次编号，例如：

```text
lecture01-intermediate-econometrics-introduction.pptx
lecture02-regression-review.pdf
```

这样可以避免中文、空格和括号导致链接失效。更新同一讲时优先沿用原文件名，让学生收藏的下载地址保持不变。

上传后的处理步骤：

1. 检查PPTX或PDF能否正常打开、页数是否合理；
2. 将文件复制到固定目录；
3. 在 `course/updates.qmd` 增加或更新下载入口与日期；
4. 补充材料需要独立说明时，新建一个仓库专用 `.qmd` 页面；
5. 新页面若要进入导航和Agent索引，将其加入 `EXTRA_RESOURCE_PAGES`；
6. 完整构建并检查公网链接。

课件文件默认作为下载材料。若希望AI Agent直接理解课件内容，最好同时提供对应的Markdown讲义或Quarto网页版本；只有PPTX或PDF时，不同Agent的读取效果可能不一致。

## 7. 更新代码和数据

**学生用的代码与数据在独立仓库**：<https://github.com/zhang-chenglei/Econometrics-course-materials>。
本仓库的 `materials/` 只保留随站发布的内容：`data/`（教学数据，供 `course/materials.qmd` 直链下载）和 `slides/`（课堂讲义）。**本仓库不再存放学生代码。**

### 学生仓库是怎么来的

它由 `tools/sync_student_repo.py` 生成，**不要手工编辑学生仓库**：

```bash
# 改完 EconKB 源文件后，在本仓库执行
python3 tools/sync_student_repo.py \
  --econkb ~/Documents/EconKB \
  --target ~/Documents/EconKB/30_教学/09_计量教材/Econometrics-course-materials
```

源文件位置：

- 第1—9章：EconKB `30_教学/09_计量教材/code/{python,stata,ai_cards}/`
- 第11—14章综合案例：EconKB `30_教学/09_计量教材/code/comprehensive_case/`
- 数据：本仓库 `materials/data/`

脚本按教材章节重组、剔除 `legacy/` 旧版文件、把任务卡统一命名为 `AI任务卡.md`、生成 `README.md`，并让脚本与学生源**逐字一致**——两边靠生成关系保持同步，不靠人工纪律。

### 学生版代码必须自包含

学生会把脚本下载到桌面、下载文件夹或U盘再运行，因此：

- 不得读取仓库内部目录（`图片/`、`教材插图` 等）——学生手里没有这些目录；
- 不得依赖脚本所在的目录深度（如 `Path(__file__).parents[3]`）——这类写法不报错，而是在学生解压的位置旁边**凭空新建一个目录**；
- 输出只写在自己脚本同级的 `output/`。

发布前必须确认：

- 没有学生姓名、学号、联系方式等个人信息；
- 没有API密钥、账号密码和本机配置；
- 没有受许可限制、禁止再分发的数据；
- 代码路径不依赖个人电脑的绝对地址，也不依赖仓库内的目录层级；
- 数据和代码至少有一份README说明来源、变量和运行顺序；
- 相关入口已同步更新到 `course/materials.qmd`。

可自动检查：

```bash
python3 tools/check_materials_code.py            # 绝对路径、仓库内部目录 → 报错
python3 tools/check_materials_code.py --strict   # 目录深度依赖也一并报错
python3 tools/sync_student_repo.py --econkb ~/Documents/EconKB --target <学生仓库> --check
```

`check_materials_code.py` 默认扫描同级目录下的 `Econometrics-course-materials`，可用 `--target` 指定其他检出位置。

## 8. 新增GitHub专用网页

新页面放在 `course/`，扩展名使用 `.qmd`。如果页面只属于GitHub网站而不属于飞书课程，必须：

1. 在 `tools/sync_from_econkb.py` 的 `EXTRA_RESOURCE_PAGES` 中登记；
2. 需要时在仓库专用的 `course/updates.qmd` 增加入口；若要修改自动生成的 `course/resources.qmd`，必须先修改其飞书课程版源文件再同步；
3. 重新生成 `llms.txt`；
4. 验证同步脚本不会把它视为意外孤立页面。

不要手工修改 `_quarto.yml` 的自动生成区；自动生成区以外的站点标题、顶部导航和样式配置可以按需要修改。

## 9. 本地构建与检查

正式发布前至少执行：

```bash
quarto render
git diff --check
git status --short
```

验收重点：

- Quarto完整构建成功；
- 新页面存在于 `_site/`；
- 页面中的站内链接指向生成后的 `.html`；
- PDF、PPT、代码和数据被复制到 `_site/materials/`；
- 桌面端和手机端导航没有明显错位；
- 页面没有残留本机绝对路径；
- 下载文件可以从本地构建结果正常读取。

如需边改边看，可运行：

```bash
quarto preview
```

## 10. 提交与发布

确认改动无误后：

```bash
git add <本次修改的文件>
git commit -m "简明说明本次更新"
git push origin main
```

不要用 `git add .` 草率提交不明文件。推送后可查看发布状态：

```bash
gh run list --limit 3
```

只有GitHub Actions显示 `success`，并且公开页面与下载链接返回正常，才算发布完成。

## 11. 出错与回退

- 先用 `git diff` 和 `git log --oneline` 确认问题来自哪次修改。
- 已经推送的错误提交优先使用 `git revert <commit>` 生成可追溯的回退提交。
- 不使用 `git reset --hard`、强制推送或批量删除来处理普通发布错误。
- 如果同步结果异常，先停止提交，检查权威源、`manifest.json` 和同步脚本，不要在大量生成文件上逐个手修。

## 12. Agent接手清单

Codex或Claude Code开始工作时，应按以下顺序：

1. 阅读 `维护指南.md` 和 `README.md`；
2. 查看 `git status`、当前分支、远端与最近提交；
3. 判断任务属于权威源同步还是仓库专用内容；
4. 修改前向用户说明范围、风险和验证方式；
5. 只改与任务相关的文件；
6. 运行同步检查、Quarto构建和链接验证；
7. 用户要求发布时才提交并推送；
8. 发布后核验GitHub Actions与公开网址；
9. 在EconKB当天日志中记录重要结构调整和发布结果。

## 13. 当前关键入口

- 课程主页：<https://zhang-chenglei.github.io/Econometrics-course/>
- 课件与更新：<https://zhang-chenglei.github.io/Econometrics-course/course/updates.html>
- 代码与数据：<https://zhang-chenglei.github.io/Econometrics-course/course/materials.html>
- 2025—2026秋季学期计量基础课件：<https://zhang-chenglei.github.io/Econometrics-course/course/basic_econometrics_slides.html>
- GitHub提交记录：<https://github.com/zhang-chenglei/Econometrics-course/commits/main/>

最后更新：2026-09-14。
